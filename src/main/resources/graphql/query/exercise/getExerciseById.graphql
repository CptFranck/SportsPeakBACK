query ($id : ID!){
    getExerciseById(id: $id) {
        id
        exerciseTypes {
            id
            name
            goal
        }
        muscles {
            id
            name
            function
        }
        name
        description
        goal
    }
}