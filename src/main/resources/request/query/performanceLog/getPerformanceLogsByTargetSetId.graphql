query ($targetSetId : Int!){
    getPerformanceLogsByTargetSetsId(targetSetId: $targetSetId) {
        id
        id
        setIndex
        repetitionNumber
        weight
        weightUnit
        logDate
        targetSet {
            id
        }
    }
}