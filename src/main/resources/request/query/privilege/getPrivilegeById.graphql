query ($id : ID!) {
    getPrivilegeById (id: $id) {
        id
        name
        roles {
            id
            name
            privileges {
                id
                name
            }
        }
    }
}