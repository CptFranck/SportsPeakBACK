query ($id : Int!) {
    getPrivilegeById (id: $id) {
        id
        name
        roles {
            id
            name
            privileges {
                id
                name
            }
        }
    }
}