query ($id : Int!) {
    getMuscleById (id : $id) {
        id
        name
        latinName
        function
        description
        illustrationPath
        exercises {
            id
            name
            goal
        }
    }
}