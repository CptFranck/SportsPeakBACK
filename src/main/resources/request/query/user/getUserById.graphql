query ($id: ID!) {
    getUserById(id: $id) {
        id
        email
        firstName
        lastName
        username
        roles {
            id
            name
            privileges {
                id
                name
            }
        }
    }
}