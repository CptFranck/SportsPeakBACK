mutation ($inputNewRole : InputNewRole!){
    addRole(inputNewRole: $inputNewRole) {
        id
        name
        users {
            id
            email
            firstName
            lastName
            username
        }
        privileges {
            id
            name
        }
    }
}