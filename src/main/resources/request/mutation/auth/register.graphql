mutation ($inputNewUser : InputNewUser!) {
    register(inputNewUser: $inputNewUser){
        tokenType
        accessToken
        user {
            id
            email
            firstName
            lastName
            username
            userRole
        }
    }
}