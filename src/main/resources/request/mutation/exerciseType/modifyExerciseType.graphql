mutation ($inputExerciseType : InputExerciseType!){
    modifyExerciseType(inputExerciseType: $inputExerciseType) {
        id
        name
        goal
        exercises {
            id
            name
            goal
        }
    }
}