mutation ($inputUser : InputUser!){
    modifyUser(inputUser: $inputUser) {
        id
        email
        firstName
        lastName
        username
        roles {
            id
            name
            privileges {
                id
                name
            }
        }
    }
}